export class Stock {
  public shares: number = 1;
  constructor(public name: string, public total: string, public republicans: string, public democrats: string, public indivs: string, public pacs: string, public cycle: string) { }
}
